{
  "name": "@trixis/lib-ts-bot",
  "version": "0.2.20",
  "description": "Package for Discord bots micro-framework template - https://github.com/TrixiS/base-ts-bot",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "build": "rm ./dist -rf && pnpm tsc",
    "p": "pnpm run build && pnpm version patch && pnpm publish --access=public"
  },
  "dependencies": {
    "discord.js": "14.6.0",
    "zod": "^3.19.1"
  },
  "devDependencies": {
    "@types/node": "^18.11.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.8.4"
  }
}
