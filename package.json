{
  "name": "@trixis/lib-ts-bot",
  "version": "0.1.20",
  "description": "Package for Discord bots micro-framework template - https://github.com/TrixiS/base-ts-bot",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "/dist"
  ],
  "scripts": {
    "p": "rm ./dist -rf && pnpm tsc && pnpm version patch && pnpm publish --access=public"
  },
  "sdependencies": {
    "discord.js": "^14.2.0"
  },
  "devDependencies": {
    "@types/node": "^18.7.3",
    "typescript": "^4.7.4"
  }
}
